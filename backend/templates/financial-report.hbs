<div class="table-section">
    <h2 class="table-title">Financial Summary</h2>
    <div class="summary-cards">
        <div class="summary-card">
            <h4>Total Premium Revenue</h4>
            <div class="value">${{formatCurrency totalPremiumRevenue}}</div>
        </div>
        <div class="summary-card">
            <h4>Total Claims Paid</h4>
            <div class="value">${{formatCurrency totalClaimsPaid}}</div>
        </div>
        <div class="summary-card">
            <h4>Net Profit</h4>
            <div class="value">${{formatCurrency netProfit}}</div>
        </div>
        <div class="summary-card">
            <h4>Profit Margin</h4>
        <div class="value">{{profitMargin}}%</div>
    </div>
</div>

<div class="table-section cols-6">
    <h2 class="table-title">Revenue and Expenses Breakdown</h2>
    <table class="data-table">
    <thead>
        <tr>
            <th>Category</th>
            <th>Amount</th>
            <th>Percentage of Total</th>
            <th>Change from Previous Period</th>
        </tr>
    </thead>
    <tbody>
        {{#each revenueBreakdown}}
        <tr>
            <td>{{category}}</td>
            <td>${{formatCurrency amount}}</td>
            <td>{{percentage}}%</td>
            <td>{{changeFromPrevious}}%</td>
        </tr>
        {{/each}}
    </tbody>
</table>
</div>

{{#if monthlyFinancials}}
<div class="page-break"></div>
<div class="table-section cols-6">
    <h2 class="table-title">Monthly Financial Performance</h2>
    <table class="data-table">
    <thead>
        <tr>
            <th>Month</th>
            <th>Premium Revenue</th>
            <th>Claims Paid</th>
            <th>Operating Expenses</th>
            <th>Net Income</th>
            <th>Loss Ratio</th>
        </tr>
    </thead>
    <tbody>
        {{#each monthlyFinancials}}
        <tr>
            <td>{{month}}</td>
            <td>${{formatCurrency premiumRevenue}}</td>
            <td>${{formatCurrency claimsPaid}}</td>
            <td>${{formatCurrency operatingExpenses}}</td>
            <td>${{formatCurrency netIncome}}</td>
            <td>{{lossRatio}}%</td>
        </tr>
        {{/each}}
    </tbody>
</table>
</div>
{{/if}}

{{#if policyTypeRevenue}}
<div class="table-section cols-6">
    <h2 class="table-title">Revenue by Policy Type</h2>
    <table class="data-table">
    <thead>
        <tr>
            <th>Policy Type</th>
            <th>Premium Revenue</th>
            <th>Number of Policies</th>
            <th>Average Premium</th>
            <th>Market Share</th>
        </tr>
    </thead>
    <tbody>
        {{#each policyTypeRevenue}}
        <tr>
            <td>{{policyType}}</td>
            <td>${{formatCurrency premiumRevenue}}</td>
            <td>{{numberOfPolicies}}</td>
            <td>${{formatCurrency averagePremium}}</td>
            <td>{{marketShare}}%</td>
        </tr>
        {{/each}}
    </tbody>
</table>
</div>
{{/if}}

{{#if agentPerformance}}
<div class="page-break"></div>
<div class="table-section cols-7">
    <h2 class="table-title">Agent Performance</h2>
    <table class="data-table">
    <thead>
        <tr>
            <th>Agent Name</th>
            <th>Policies Sold</th>
            <th>Premium Generated</th>
            <th>Commission Earned</th>
            <th>Customer Satisfaction</th>
        </tr>
    </thead>
    <tbody>
        {{#each agentPerformance}}
        <tr>
            <td>{{agentName}}</td>
            <td>{{policiesSold}}</td>
            <td>${{formatCurrency premiumGenerated}}</td>
            <td>${{formatCurrency commissionEarned}}</td>
            <td>{{customerSatisfaction}}/5</td>
        </tr>
        {{/each}}
    </tbody>
</table>
</div>
{{/if}}

{{#if keyMetrics}}
<div class="table-section cols-7">
    <h2 class="table-title">Key Performance Indicators</h2>
    <table class="data-table">
    <thead>
        <tr>
            <th>Metric</th>
            <th>Current Period</th>
            <th>Previous Period</th>
            <th>Change</th>
            <th>Target</th>
            <th>Performance</th>
        </tr>
    </thead>
    <tbody>
        {{#each keyMetrics}}
        <tr>
            <td>{{metricName}}</td>
            <td>{{currentPeriod}}</td>
            <td>{{previousPeriod}}</td>
            <td>{{change}}%</td>
            <td>{{target}}</td>
            <td><span class="status {{performanceStatus}}">{{performanceLabel}}</span></td>
        </tr>
        {{/each}}
    </tbody>
</table>
</div>
{{/if}}